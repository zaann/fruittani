import 'package:flutter/material.dart';

class HamaPenyakitPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hama dan Penyakit'),
      ),
      body: Center(
        child: Text('Halaman Hama dan Penyakit'),
      ),
    );
  }
}
